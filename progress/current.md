# Sesión activa

> El líder mantiene este archivo. Se limpia al cerrar cada feature.

**Feature en curso:** ninguna
**Plan:** —
**Estado:** F001 y F002 cerradas (APROBADO, `./init.sh` verde 30 ok). Siguiente `pending`: **F003**
(pipeline de contrato: OpenAPI de Ninja → tipos TS; cliente fetch tipado; home consume /api/health).
Capas: backend + frontend. No requiere herramientas nuevas (uv + pnpm ya están).
